# RBAC 미들웨어(#2) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** GUEST/USER/SUSPENDED/ADMIN 게이트와 SUSPENDED 전면차단(초크포인트+실시간)을 재사용 프리미티브로 구현한다.

**Architecture:** `src/features/auth/rbac.ts`에 authorize 프리미티브(DB fresh role 기준). SUSPENDED를 세션 발급 초크포인트(login/oauth/refresh)에서 차단 + 인증 라우트 실시간 검사. 마이그레이션·전용 UI 없음.

**Tech Stack:** Next.js 16, Prisma 7, zod 4, Vitest + Playwright. 재사용: `currentUserFromRefresh`, `getCurrentUser`, `AppError`, `withErrorHandling`, `logAuthEvent`.

**설계:** `docs/superpowers/specs/2026-07-24-rbac-design.md`.

## Global Constraints
- **역할 원천은 DB fresh**(토큰 role은 최대 15분 stale) — authorize는 DB에서 role·deletedAt 읽는다.
- **SUSPENDED 전면차단:** login·oauth로그인·refresh 회전에서 세션 발급 거부(ACCOUNT_SUSPENDED 403) + 인증 라우트 실시간 403. soft-delete도 동일 차단.
- RBAC 실패는 `AppError` 코드만(UNAUTHENTICATED 401 / ACCOUNT_SUSPENDED 403 / FORBIDDEN 403) — 내부·PII 노출 없음. 공개 라우트(공개 프로필·헬스)는 무인증 유지.
- 기존 인증 로직(실패 일반화·재사용 감지·2FA 게이트) 회귀 없이 조건만 추가.
- TypeScript strict. 짧은 한글 커밋, Co-Authored-By 금지. 브랜치 `feat/rbac`. Node 빌트인 테스트 `// @vitest-environment node`.

## 실행 카덴스
🔴 적대적 리뷰(프리미티브·초크포인트·라우트). 🟢 메인 점검(E2E·카탈로그). 최종 opus.

## File Structure
| 파일 | 책임 | 태스크 |
|---|---|---|
| `src/features/auth/rbac.ts` | authorize 프리미티브 | 1 |
| `src/features/auth/login.ts`·`oauth/link.ts`·`session.ts`(수정) | SUSPENDED 초크포인트 | 1 |
| 기존 인증 mutating 라우트(수정)·`src/app/api/admin/ping/route.ts` | requireActiveUser 적용·admin 게이트 | 2 |
| `src/i18n/messages/*`(수정) | 정지 에러 카탈로그 | 2 |
| `e2e/rbac.spec.ts`·워크로그 | E2E·기록 | 3 |

---

### Task 1 🔴: RBAC 프리미티브 + SUSPENDED 초크포인트

**Files:** Create `src/features/auth/rbac.ts` + test; Modify `login.ts`(+test), `oauth/link.ts`(+test), `session.ts`(+test).

**Interfaces:** `Role`, `assertNotSuspended(user)`, `assertRole(role, allowed)`, `requireActiveUser(db, req)`, `requireAdmin(db, req)`, `requireActiveBearer(db, req)`.

- [ ] **Step 1: rbac 테스트(RED)** `rbac.test.ts`(목 AuthDb, `// @vitest-environment node`):
  - `assertNotSuspended`: `{role:"USER", deletedAt:null}`→통과, `{role:"SUSPENDED",...}`→`ACCOUNT_SUSPENDED` 403, `{role:"USER", deletedAt:Date}`→`ACCOUNT_GONE` 403.
  - `assertRole`: `("ADMIN",["ADMIN"])`→통과, `("USER",["ADMIN"])`→`FORBIDDEN` 403.
  - `requireActiveUser`: 유효 refresh 세션+USER→`{userId, role:"USER"}`; SUSPENDED 유저(DB fresh)→403; deletedAt→403; 쿠키 없음/무효 세션→`UNAUTHENTICATED` 401. **토큰 role이 USER라도 DB가 SUSPENDED면 403**(fresh 우선) 단언.
  - `requireAdmin`: ADMIN→`{userId}`, USER→403.

- [ ] **Step 2: rbac 구현** `src/features/auth/rbac.ts`:
```ts
import { AppError } from "@/features/_shared/error";
import { currentUserFromRefresh } from "./session";
import { readRefreshCookie } from "./cookies";
import { getCurrentUser } from "./context";
import type { AuthDb } from "./db";

export type Role = "USER" | "SUSPENDED" | "ADMIN";

export function assertNotSuspended(user: { role: Role; deletedAt: Date | null }): void {
  if (user.deletedAt) throw new AppError("ACCOUNT_GONE", "이용할 수 없는 계정이에요.", 403);
  if (user.role === "SUSPENDED") throw new AppError("ACCOUNT_SUSPENDED", "계정이 정지되었어요.", 403);
}

export function assertRole(role: Role, allowed: Role[]): void {
  if (!allowed.includes(role)) throw new AppError("FORBIDDEN", "권한이 없어요.", 403);
}

async function loadActive(db: AuthDb, userId: string): Promise<{ userId: string; role: Role }> {
  const user = await db.user.findUnique({ where: { id: userId }, select: { role: true, deletedAt: true } });
  if (!user) throw new AppError("UNAUTHENTICATED", "로그인이 필요해요.", 401);
  assertNotSuspended(user as { role: Role; deletedAt: Date | null });
  return { userId, role: user.role as Role };
}

export async function requireActiveUser(db: AuthDb, req: Request): Promise<{ userId: string; role: Role }> {
  const current = await currentUserFromRefresh(db, readRefreshCookie(req));
  if (!current) throw new AppError("UNAUTHENTICATED", "로그인이 필요해요.", 401);
  return loadActive(db, current.userId);
}

export async function requireActiveBearer(db: AuthDb, req: Request): Promise<{ userId: string; role: Role }> {
  const claims = await getCurrentUser(req);
  if (!claims) throw new AppError("UNAUTHENTICATED", "로그인이 필요해요.", 401);
  return loadActive(db, claims.userId);
}

export async function requireAdmin(db: AuthDb, req: Request): Promise<{ userId: string }> {
  const u = await requireActiveUser(db, req);
  assertRole(u.role, ["ADMIN"]);
  return { userId: u.userId };
}
```

- [ ] **Step 3: 초크포인트** — 각 테스트에 SUSPENDED 케이스 추가 후:
  - `login.ts`: user select에 `role`·`deletedAt` 추가(이미 deletedAt 있을 수 있음). 비번 검증 성공 후 **2FA 챌린지·세션 발급 전에** `if (user.role === "SUSPENDED" || user.deletedAt) throw new AppError("ACCOUNT_SUSPENDED", "계정이 정지되었어요.", 403)`. (없는계정/오비번 일반화는 그대로 — 정지는 인증 통과 후이므로 열거 위험 없음.)
  - `oauth/link.ts` `loginOrRegisterWithOAuth`: 기존 신원 로그인 분기(deletedAt 검사 옆)에 `role==="SUSPENDED"` 차단.
  - `session.ts` `rotateSession`: `current.user.deletedAt` 검사에 `|| current.user.role === "SUSPENDED"` 추가(select에 role 추가), 해당 시 `authFailed` 대신… — 기존은 `authFailed()`(AUTH_FAILED 401). 정지는 폐기+`ACCOUNT_SUSPENDED` 403이 명확하나, 회전 실패를 401로 통일해도 무방(재로그인도 막히므로). **결정:** rotate에서 SUSPENDED면 해당 세션 폐기 + `AppError("ACCOUNT_SUSPENDED", 403)`. select에 `role` 추가.

- [ ] **Step 4: 통과·커밋**
```bash
export PATH="/c/Program Files/nodejs:$PATH"
node node_modules/vitest/vitest.mjs run && node node_modules/typescript/bin/tsc --noEmit
git add src/features/auth/rbac.ts src/features/auth/rbac.test.ts src/features/auth/login.ts src/features/auth/login.test.ts src/features/auth/oauth/link.ts src/features/auth/oauth/link.test.ts src/features/auth/session.ts src/features/auth/session.test.ts
git commit -m "RBAC 프리미티브와 SUSPENDED 세션발급 초크포인트 추가"
```

---

### Task 2 🔴: 라우트 적용 + admin 게이트

**Files:** Modify 인증 mutating 라우트들(공통); Create `src/app/api/admin/ping/route.ts`; Modify catalogs.

- [ ] **Step 1: admin 게이트 라우트** `src/app/api/admin/ping/route.ts`(JSON, withErrorHandling): `requireAdmin(prisma, req)` → `Response.json({ ok: true, role: "ADMIN" })`. (ADMIN만 200, USER/SUSPENDED 403, GUEST 401.)

- [ ] **Step 2: 인증 라우트에 active-user 게이트 적용** — 아래 mutating 라우트들에서 `currentUserFromRefresh`(+ 미인증 401) 패턴을 `requireActiveUser(prisma, req)`로 교체(정지·삭제 실시간 차단). step-up 게이트 라우트는 `requireActiveUser` 먼저 → 그 다음 requireRecentAuth. 대상(공개·헬스 제외):
  - `/api/profile/bio`, `/api/profile/nickname`, `/api/auth/password/{set,change}`, `/api/auth/withdraw`, `/api/auth/location`, `/api/auth/phone/{send,verify}`, `/api/auth/oauth/[provider]/unlink`, `/api/auth/2fa/{totp,email}/*`·`/api/auth/2fa/disable`, `/api/auth/step-up`·`/step-up/send-otp`.
  - `/api/profile/me`: bearer 아님(쿠키)이면 requireActiveUser; bearer면 requireActiveBearer. (현 구현 확인 후 맞춰 교체.)
  - **주의:** 각 라우트의 나머지 로직(step_up userId 일치 등)은 불변. `requireActiveUser`가 반환하는 userId를 기존 current.userId 자리에 사용. userId 일치 검사(step-up)는 그대로.
  - **공개 유지:** `/api/profile/[nickname]`, `/api/health`는 건드리지 않음.

- [ ] **Step 3: 카탈로그** — `common.suspended`("계정이 정지되었어요. 문의해 주세요"), `common.forbidden`("권한이 없어요") 양 로케일. (폼들이 에러 코드 매핑에 추가 — 최소.)

- [ ] **Step 4: 수동/단위 검증** — 라우트 단위 테스트가 있으면 SUSPENDED 유저(목)로 403 확인. admin/ping: ADMIN 200·USER 403·GUEST 401.
```bash
node node_modules/vitest/vitest.mjs run && node node_modules/typescript/bin/tsc --noEmit
git add src/app/api src/i18n/messages
git commit -m "인증 라우트에 active-user 게이트와 admin 게이트 적용"
```

---

### Task 3 🟢: E2E + 워크로그

**Files:** Create `e2e/rbac.spec.ts`, `docs/worklog/2026-07-24-rbac.md`.

- [ ] **Step 1: E2E** — `test.use({locale:"ko-KR"})`, unique 유저. DB로 role을 직접 바꾸는 것은 psql로(테스트 내 `docker compose exec`는 어려우므로 API 없음 → **관리 API 부재**를 감안: E2E는 (a) GUEST가 보호 라우트 401, (b) 정상 유저 mutating 200, (c) admin/ping이 USER 403·GUEST 401, (d) 정지 시나리오는 psql UPDATE로 role=SUSPENDED 후 같은 세션 mutating 403 + refresh 403 + 재로그인 403). psql UPDATE는 검증 스텝(테스트 격리 위해 unique 유저).
  - 만약 E2E에서 psql 접근이 어렵다면 정지 시나리오는 **라우트/서비스 단위 테스트로 커버**하고 E2E는 GUEST/USER/admin 게이트만. 워크로그에 명시.

- [ ] **Step 2: 실행·점검**
```bash
export PATH="/c/Program Files/nodejs:$PATH"
docker compose up -d db && node node_modules/prisma/build/index.js migrate deploy && node node_modules/@playwright/test/cli.js test
node node_modules/typescript/bin/tsc --noEmit && node node_modules/vitest/vitest.mjs run && node node_modules/next/dist/bin/next build
```
Expected: 전 스펙 green.

- [ ] **Step 3: 워크로그·커밋** — 형식(무엇을/왜/결정/편차, 태스크표, **검토중점**(SUSPENDED 두겹 차단·DB fresh role·에러마스킹·공개라우트 회귀없음), DoD 6항). 커밋:
```bash
git add e2e/rbac.spec.ts docs/worklog/2026-07-24-rbac.md
git commit -m "RBAC E2E와 워크로그 추가"
```

---

## DoD — 설계 G절과 동일 (1~6)
## 범위 밖 — 관리자 제재 API/UI/대시보드 → #6. 리소스 소유권 권한 → #3~#5.
